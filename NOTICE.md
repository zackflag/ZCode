# ZCode Feature Notes and Third-Party Component Notices

<p align="center">
  English | <a href="NOTICE.zh-CN.md">简体中文</a>
</p>

This notice applies to the source code and build artifacts published in this repository. Features, permissions, storage locations and network behavior differ between runtimes; the defaults of one runtime must not be read as the project-wide settings.

## 1. AI Output, Execution Permissions and Automation Risks

AI-generated code, commands, explanations, files and suggestions may contain errors, omissions, security defects or third-party rights issues. Verify them against actual files, command exit statuses, tool results and tests; a model claiming something is "done", "handled safely" or "authorized" is not proof of fact or authorization. Before important operations, review the target, recipients and impact, and keep recoverable backups.

Files, terminals, Git, agent tools and external processes can read and write files, start processes and access the network within the permissions of the operating-system account that runs them. In Web or remote workspaces, operations may happen on the server, SSH host, WSL or container environment; do not infer the execution location from the device showing the page. The current shared agent execution adapter does not provide an OS sandbox by default; working directories, workspace identity, Git worktrees, browser page isolation and the Node REPL context are not system-level isolation guarantees for all tools.

Shared run configurations default to the `build` permission mode; when the standalone CLI runs a non-interactive task via `--prompt` without `--mode`, it uses `yolo`. Interactive sessions, desktop sessions, restored tasks and hosts may use their own configured, saved or passed modes. Whether each action is confirmed depends on tool declarations, permission rules and the run mode; `yolo` without a read-only planning constraint lets ordinary tool operations through, while interactive tools and tools that explicitly require confirmation keep their own rules. Model tool approval is not a single global permission switch for the entire app; terminal operations, plugin processes and update downloads must not be assumed to go through the same approval flow. See [CLI prompt modes](apps/zcode-cli/packages/cli/src/run.ts#L486) and [tool permission checks](apps/zcode-cli/packages/core/src/permission/service.ts#L97).

| Capability or hook | Possible impact and boundaries |
| --- | --- |
| Files, Git, terminal, Shell, Node REPL | Can read, write or delete data, run programs and access the network; Git, package managers and user commands may also trigger their own scripts and hooks. Task snapshots, Git checkpoints and session restore do not replace backups of all local files, databases and external services. |
| User and workspace instructions, skills | Workspace instruction files, skill text, templates and scripts can influence model plans; reading, installing or displaying them does not mean the content is trustworthy, lawful or reviewed by the vendor for security. |
| Lifecycle hooks | `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PostToolUseFailure` and `Stop` can trigger commands or processes. Inputs may include prompts, working paths, tool arguments, tool results and replies; hooks can also add context, rewrite tool inputs or take part in permission decisions. |
| Workspace hook trust | Workspace hooks are reviewed per workspace identity and declaration digest; a change to the declaration invalidates the previous trust. This mechanism is not a unified review of plugin hooks, MCP configuration or other project scripts. |
| Plugins and MCP | Enabling plugins can bring in hooks that run automatically, local programs and remote tools. The current agent run configuration includes workspace-level MCP in the auto-connect scope; with MCP enabled and the runtime started, it may use the configured commands, environment variables, auth headers or OAuth to connect to services. Plugin hooks and workspace hooks use different admission paths. |
| Startup and install scripts | The desktop app or terminal may execute login shells and their startup configuration to obtain the shell environment; dependency installation may also run lifecycle scripts such as native module builds. These scripts can run before model tasks and should not be understood as only executing commands the model approved one by one. |
| Subagents, workflows and background tasks | Can keep calling models, running commands and incurring costs or external side effects. Stopping a session, switching pages or cancelling a request cannot undo completed file writes, releases, uploads or other external operations. |
| Desktop scheduled and off-peak tasks | Run under the stored configuration when the scheduler and execution host are available, and can continue while the window is hidden. Closing the window may only hide it to the background or tray; explicit quit, sleep, network loss, account or model unavailability may all affect execution. There is no guarantee that tasks keep running after shutdown or that missed tasks always catch up; the standalone CLI does not gain a persistent scheduling service just because it includes a Cron tool. |
| Embedded browser and Browser Use | Can visit pages, read page content, take screenshots or record pages, and perform clicks, typing, uploads and downloads. Browser sign-in state imported or reused through user actions may allow access to private data, form submissions or changes to remote accounts; visiting a page itself communicates with the site and the services it loads. |
| Computer Use | The Computer Use package shipped in this repository is an unavailable placeholder; calls return an unavailable error and provide no system screenshot or control capability. See the [Computer Use entry](packages/zcode-cua/index.js#L3). |
| Web services and remote runtime | Reachability, sign-in authentication, process accounts and file permissions are determined by the specific deployment. Authentication defaults vary by entry point and listening address; local listening, origin checks and SSH channels each control different boundaries, and development services or local core ports should not be treated as suitable for public exposure. |
| Content sharing, importing and resource previews | May read or transmit conversations, code, attachments and generated artifacts. Imported shared context, web pages and files are external inputs; a source displayed as a ZCode link does not make its content a trusted instruction or grant a copyright licence to it. |

Web pages, repository content, tool return values and third-party instructions may contain content that induces the model to perform extra operations. When handling untrusted projects, restrict the running account, credentials and network permissions, and review sensitive operations.

When self-hosting Web or remote services, configure authentication, transport protection and access control per entry point; application account sign-in does not replace deployment-level security boundaries. See the [README run instructions](README.md#zcode-cli-distribution) for Web startup parameters and authentication configuration. The standalone remote service core only accepts loopback listening addresses and does not provide equivalent external account authentication. HTTP/WS and HTTPS/WSS, and SSH, protect traffic under different conditions; not all deployments are guaranteed to be encrypted by default, and remote hosts or service operators are not guaranteed to be unable to access the data they process. See the [listening rules](packages/zcode-server-cli/src/server-core/http.ts#L115) for the remote core's limits.

## 2. Upload Endpoints, Outbound Requests and Business Purposes

| Scenario | Purpose, trigger and data scope |
| --- | --- |
| Model and auxiliary model requests | Task execution, compaction, titles, memory, history extraction, goal-completion verification, AI Git commit messages and model connectivity tests can call models; some auxiliary calls fire along with tasks. They send the relevant prompts, history, code, diffs, tool results or attachments; the exact scope varies by feature, and connectivity tests use a fixed probe message. Requests may also carry authentication, client environment and session/request/trace identifiers; Anthropic-compatible calls add device and session identifiers. The recipient depends on the actual endpoint, the gateway forwarding below and proxy configuration; the SDK name alone does not determine it. |
| Sign-in, authorization, accounts and API credentials | Sign-in, code exchange, sign-in polling and account/entitlement queries can send authorization status, codes, access tokens, polling credentials and client identifiers; after sign-in or during account configuration resolution, project API keys may be queried, fetched or created automatically, and subscribed team projects may also have missing keys created during pricing refreshes. The embedded plan page can pass sign-in credentials, language and theme to origin-checked pages, along with device ID, account ID and app version. The built-in account adapter currently does not implement refresh-token exchange; automatic MCP OAuth refresh is covered under "MCP connections, OAuth and tool calls". Account authentication proves access identity; it does not mean every subsequent send has separate user confirmation; the internal handling of external authorization pages is not verified in this repository. |
| Plans, orders, payments and quotas | Quota resets and related status sync can send account credentials, personal or team/project scope, reset type and idempotency identifiers to business services; opportunity requests, status polling and read receipts may fire automatically. The repository provides registered order estimate/create/cancel, renewal, payment-method management and payment services; calls may send product, order, payment-method or authorization identifiers, amounts, subscription options and caller-provided channel or tracking fields. The existence of a service method does not mean every distribution has a usable purchase UI, nor that opening a page starts a payment. |
| Official Coding Plan gateway forwarding | The two official Anthropic-compatible model endpoints listed in the code are matched by protocol, host, effective port and path; on a match, requests are automatically re-sent to the ZCode gateway, keeping the method, body, query parameters and all headers except Host, including authentication in the request. The actual gateway origin can be changed by ZCODE_BASE_URL / ZCODE_ENDPOINT_ORIGIN configuration. This forwarding has no additional per-request user confirmation; the decision is based on the request URL, and a user-created provider must not be assumed to bypass the gateway. What the gateway does after receiving a request is outside the client source code verified here. |
| Providers, business configuration and connectivity checks | Background checks, configuration refreshes, service checks or related page activity after startup can reach the provider directory, business configuration, help content and CDN, sending request URLs, version, platform and per-interface origin information; help configuration requests may also carry a device ID, and service connectivity checks may carry configured auth headers. Provider directory downloads and public configuration reads do not inherit control-plane authentication. Some requests run independently of model tasks and do not require a manual refresh each time. Model connectivity tests use the model request path; the existence of a check method does not mean all checks run automatically at startup. |
| Search, web processing and remote resources | Model tools, page visits by the user or resource display can send queries, URLs, headers and ordinary network information to model providers, target sites and resource services. WebSearch currently runs through model providers that support native search; content fetched by WebFetch may also be sent to the model together with processing prompts, so a web download should not be assumed to stay local afterwards. Recipients of web pages, marketplace images and other remote resources can also obtain access requests and IP information. |
| MCP connections, OAuth and tool calls | With MCP enabled and the corresponding configuration loaded, the runtime can automatically connect to services declared by the workspace or plugins, negotiate protocols and discover tools; OAuth additionally performs discovery, client registration, authorization exchange and automatic token refresh. Tool calls send arguments and may automatically attach the workspace absolute path, workspace identity, session/turn/trace and remote session identifiers. HTTP/SSE sends to the configured service; stdio hands off to a process on the local or execution host, whose later network behavior depends on that program. Tool approval does not uniformly cover connection, discovery and refresh flows. |
| SSH / WSL / containers and other execution environments | After a connection is established or a task runs, protocol commands, prompts, files, attachments, tool results and terminal state can cross environments. Personal provider configuration, account selection settings and syncable account API keys, OAuth access/refresh tokens and similar credentials can be passed to the target environment automatically on first provisioning and when related configuration or credentials change while the connection lives; this sync has no separate per-credential confirmation step. SSH may involve network transfer; WSL/containers may use file copies or process input; these transfers do not necessarily go to vendor cloud. |
| Skills, MCP, plugin and config sync | When selected and synced from the settings UI, skill or plugin directories, supporting files, marketplace sources and configuration can be passed to the target environment; service interfaces can also be called directly by callers. Skill sync can include files from selected directories, and MCP configuration can contain command arguments, environment variables, headers, authentication and local paths. Some plugin options filter sensitive fields, but this is not a comprehensive secret scan of directory archives or all configuration. |
| Attachment transfer and media preview | Files, images or text can come from selection, drag-and-drop, paste and task output. Once an attachment is added to the input area with an execution session, pre-transfer may start before the message is sent, and restoring a session may resume processing the queue. Local file paths can be referenced without upload; the recipient of protocol chunked uploads or remote pre-transfer is the execution host. Remote media preview may also read from that host back to the local device. Such local or cross-environment handling is not a public release; content may still enter the model or sharing paths per the task afterwards. |
| Session sharing, browsing and importing | A share action can submit a public projection of the selected session rows and materializable attachments to the sharing service, including user text, model replies and reasoning text, tool input/output and related metadata. The selection stage can first query sharing service capabilities; that capability request does not send the session projection body. The normal UI publish flow requires confirming disclosure first, and the service first verifies the caller-submitted confirmation time before creating a preparation record, uploading attachments and finally submitting the publish confirmation; attachments can therefore be sent before the final publish succeeds, but the prepare/upload steps come after the service's disclosure check. The current UI defaults to public importable, and browsing and importing do not require the publish checkbox. The confirmation-time field cannot prove that any caller went through the UI checkbox. Browsing and importing send the share code and available authentication, and access attachment download URLs; import also sends a request identifier. Projection filtering and text handling are not comprehensive secret detection; local deletion or cancellation does not guarantee that uploaded or recipient copies outside are deleted. |
| Off-peak task tickets and status sync | Availability queries, queue tickets, ticket status sync and redemption send task identifiers, ticket identifiers, origin/request identifiers, Coding Plan identity and authentication. When pending tickets and similar conditions exist, background sync can keep polling; such requests are not the same as sending the full task prompt, and actual model content goes through the model path separately. |
| Feedback, follow-up messages and diagnostic attachments | When a user submits through the feedback form, the body, contact information, account and client information, screenshots and file names can be sent; the service still provides follow-up message and attachment upload endpoints, and background tasks also use the message endpoint to report upload results — the existence of endpoints does not mean the current UI provides a follow-up editing entry. After a ticket is created the form may be collapsed, and submitted screenshots and checked logs continue uploading in background tasks; leaving the form before the ticket is created requests cancellation. Attachments first request an upload credential from the feedback service, then transfer the file directly to the returned object-storage address. The desktop log option is unchecked by default; when checked, diagnostic log files are selected by local same-day modification time and archiving rules, and the full file text is processed, with no guarantee that only same-day entries are included. Not checking logs does not affect the body or other attachments, and text-handling rules do not guarantee screenshots or arbitrary files are redacted. |
| Installation, plugin fetch, remote deployment and updates | Installation, discovery, authentication, deployment and updates reach repositories, package services, plugin marketplaces, CDNs or update servers; the unified CLI install script reads a version manifest from the configured distribution source and downloads the release package. Desktop auto-update is controlled by distribution form and runtime conditions; when enabled it can request the update manifest at startup and on periodic checks, carrying platform, channel, device and other information; platform values can distinguish architectures, and version information also affects update selection. Packaged builds ignore the `ZCODE_UPDATE_FEED_URL` environment variable and the `--zcode-update-feed-url` startup argument for update-source overrides; these two override entries are for development builds only. Downloaded code may then be installed or executed, and remote deployment may upload/copy existing or downloaded components to the target environment. Download checks, actual installation and running components have different control conditions and cannot be uniformly described as a confirmation popup at every step. |
| Delivery or publishing by tools, hooks, workflows and automations | Shell, terminal, Git, Node REPL, browser, plugins, MCP or workflows can read task-related data, upload files or artifacts to the corresponding services and change external service state; lifecycle hooks and configured scheduled/background tasks can also trigger programs. Permission rules, modes, plugin configuration and the system account determine the scope, and it is not guaranteed that every outbound payload is individually selected by the user or that every send has separate confirmation. After publishing, sending, purchasing or deploying completes, local cancellation or undo does not necessarily restore external state; permission boundaries are in section 2. |
| Dev/test, debugging and request recording | Running the related development tools explicitly can send test prompts, sample material and auth headers to configured real model or tool services; a recording proxy configured against a real upstream can forward requests, so a name containing test/recording does not mean it is fully offline. Request traces and debug files may also save model content locally; these tools have different trigger entry points than normal product runs. |
| Build, signing and maintainer releases | Running install, build or release scripts explicitly can download dependencies, native binaries and runtimes, copy build material to release shares, push Git, or hand off to signing tools and Apple notarization. External signing tools and separately configured CI, storage and distribution services have their own network behavior; processes not spelled out in this repository cannot be presumed verified just from script names. The calls have different entry points, environments and credential requirements. CLI SEA shared-directory publishing and upload verify third-party notice material first, and a failed check blocks that publish flow. The native-search preparation step verifies and unpacks repository-bundled archives per target platform and does not download them in this step; Node and other components have their own download paths. |

The business purposes above cover the interfaces provided by this project. Scripts written by users, arbitrary endpoints they configure or third-party components they install separately may have additional data-sending behavior; starting through ZCode does not by itself mean their purpose or recipient has been reviewed by the vendor.

## 3. Local Data, Logs and Credentials

Data may be stored separately on the desktop device, in the browser, on the CLI host, in SSH/WSL environments and in external services. Desktop and server data directories can be affected by the runtime environment and startup configuration, and contain settings, sessions, attachments, caches, task records, plugins and logs; the standalone CLI's default session database is at `~/.zcode/cli/db/db.sqlite`. Closing a window, signing out, uninstalling the client or deleting one workspace should not be understood as clearing all copies in other environments, standalone data directories, backups and external services.

Memory features have different defaults and entry points: the standalone CLI's default configuration enables memory, while the desktop app's setting defaults to off. When enabled, it can analyze saved sessions, persist memories and reuse them in later tasks; extraction or organization may produce extra model requests. Automation schedules, run records and unfinished tasks also have their own persistence locations, and restoring these records does not guarantee previous external operations are undone or can be fully replayed.

Shared agent model input/output logs are written locally by default in development and production runs, except in test environments. Logs may contain prompts, code, context, tool arguments and model replies; headers and some image and video data are redacted, but not all user text can be assumed redacted. Production has rotation and trimming by default; explicitly enabling full retention changes that behavior. The desktop main process, host, scheduler, Web service and remote daemon also have their own logs and crash records; a single log cap does not mean a project-wide retention period.

Credential storage mechanisms also differ: the CLI shared credential file uses encryption, with a default key derivable from local environment information, and its read logic also accepts plaintext records; desktop/Node service credentials likewise use local encrypted files with configurable or environment-derived keys, not the system keychain; Web OAuth credentials may be stored in localStorage. Model configuration, environment variables, plugin/MCP settings, SSH configuration and remotely synced data may also contain valid credentials, and these stores should not be uniformly treated as a system keychain, hardware isolation or a "never leaves the device" guarantee.

The embedded browser uses persistent browsing data; explicitly importing cookies and LocalStorage from other browsers can give it the corresponding sign-in state. Clearing the cache and clearing all site data are different operations, and browser records, downloaded files, page recordings and model logs are not necessarily removed by the same cleanup entry. Do not write real keys or tokens into files tracked by version control. Before copying logs, uploading feedback, sharing conversations or backing up data directories, check the recipient scope to avoid submitting real credentials, raw sessions and private project content to public repositories or issue reports.

## 4. Third-Party Licensing and Copyright Notices

First-party code in this repository (including all audit and modification work) is licensed under **MIT** via the root [LICENSE](LICENSE). The repository contains upstream code from [zai-org/ZCode](https://github.com/zai-org/ZCode), which stays under Apache-2.0 (full text in [LICENSE-APACHE](LICENSE-APACHE)), with copyright and attribution notices retained as-is. This licence does not grant extra rights on behalf of other right holders, nor does it cover the separate terms of third-party software, copied code, native binaries, fonts, icons, web assets and other resources. See [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) for the dependency list. Subject to third-party copyright, licensing and redistribution conditions, not all features and promotional policies of the official product are promised; the actually published source code and build artifacts prevail.
